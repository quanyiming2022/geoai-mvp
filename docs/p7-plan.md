# P7 Generic jobs
Durable PostgreSQL job records are authoritative; Redis wake hints are optional and bounded. Caller-scoped Repository/RLS, idempotency key, queued/running/succeeded/failed/cancelled, progress, retry and cancellation. Worker claims SKIP LOCKED with fencing token, runs a bounded isolated process through ComputeProvider, and never overwrites cancellation. Recover stale claims as retryable failure. Start with mock diagnostic; GeoExtract handler follows in P8. Test local SQL/RLS, concurrent claim, cancellation fence, Redis/restart recovery, UI polling, all build/tests before commit/push.
