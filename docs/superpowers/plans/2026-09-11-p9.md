# P9 Model Worker and Professional Workspace Implementation Plan

**Goal:** Preserve v0.1.0-p8; add model-independent single-tile HTTP inference, administrator-managed compute endpoints and incremental map-first UX.
**Architecture:** ModelRelease -> registered enabled ModelEndpoint -> LanHttpComputeProvider -> standalone Worker -> adapter. P8 MockAdapter and bounded-preview route stay unchanged. Real query windows are 512x512 source pixels without resampling.
**Tech Stack:** existing FastAPI/Pydantic, PostGIS/RLS, Redis jobs, Next.js/MapLibre; separate Linux CUDA adapter runtime.
**Spec:** user P9 request and docs/p9-ux-requirements.txt.

## Global constraints
- P8 = COMPLETE; frozen annotated tag v0.1.0-p8 at 6d84892daaea2b470e4ab4307008665c5847207a.
- Work only on feat/p9-real-model-worker. No main modification; stop at P9.
- Mac local platform/verification; actual CUDA inference only on user-provided LAN GPU host.
- SkySense++ research_only. Never equate fake HTTP success with real model validation.
- No AOI tiling/stitching; no resizing an AOI into a real query tile.
- Secrets remain server-side and ignored. User jobs accept endpoint IDs, never arbitrary URLs.

## Execution and gates
- [x] Freeze P8 tag and create P9 branch.
- [x] P9A contract: worker_contract.py and tests; validate exact image/mask dimensions, finite probability payload, model identity, seed and schema version.
- [x] P9A HTTP: standalone fake-real worker plus LanHttpComputeProvider; classify transport/model/OOM/invalid/cancel errors, bound retries/timeouts, propagate attempt fencing. Real localhost HTTP integration required.
- [x] P9A.5 registry: private administrator membership, model releases/endpoints with RLS, explicit operator bootstrap; CRUD/Test Connection from FastAPI, server-controlled destination validation and secret references.
- [x] Single-tile data preparation: source-resolution 512 window with affine transform; support RGB/mask matching grid; preserve existing P8 crop path. Regression tests for larger rasters and no resampling.
- [x] Jobs: opt-in endpoint/release inputs, immutable attempt identity, metrics and atomic result fencing; P8 inputs/default behavior unchanged.
- [ ] UX: design tokens, map-centric collapsible/resizable shell, layer panel, prompt tools/shortcuts, step extraction flow, bottom task progress, synchronized result inspector, Models & Compute settings and health states.
- [ ] P9B: pin official SkySense++ source and checkpoint identity; isolate composition/annotation/temporary slot/normalization/query-logits in Linux adapter. No Web/API dependency on Flood3i internals.
- [ ] Three fixtures: same support/query, same-class different query, unrelated query. Record probability mean/std, foreground ratio, mask area, runtime, GPU memory, seed, slot, endpoint/release/checkpoint. Reject degenerate real inference as success.
- [ ] Verify all P8 regressions, endpoint authorization/offline handling and browser flows at 1440x900 and 1920x1080 with screenshots.
- [ ] Commit/push verified P9 work; mark P9 COMPLETE only after actual LAN model/weights/GPU fixture validation. Otherwise report exact external blocker and measured fake-versus-real results separately.

## Required external inputs
Administrator qym57@outlook.com is configured and its browser session verified. User will supply LAN server/GPU/checkpoint later. Desktop browser QA login permission is pending; no test administrator grant is allowed. Do not use example IP as a configured server and do not promote a disposable QA user to permanent administrator.
